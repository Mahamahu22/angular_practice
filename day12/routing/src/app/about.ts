import { Component } from '@angular/core';

@Component({
  selector: 'app-about',
  standalone: true,
  template: `
    <h2>About Page</h2>
    <p>This section explains routing, guards, and resolvers in Angular...</p>
  `
})
export class About {}
